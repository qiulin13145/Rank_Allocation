结论先说：**这次结果不好，不代表 idea 错了，更像是当前实现把“rank allocation”做得太激进、太 noisy，而且 removal 过程会真实损伤模型函数。** 你现在的结果反而给了一个很清楚的信号：**小幅、低频、保守的 rank reallocation 可能不差；大幅 rank 搬运几乎一定伤。**

我读了你上传的 sweep CSV。9 个完成的 run 里，最终 PPL 大概是：

| 配置         | Final PPL   |
| ------------ | ----------- |
| delta8_top2  | **32.2389** |
| delta16_top2 | 32.3196     |
| delta8_top8  | 33.2929     |
| delta32_top8 | 33.3597     |
| delta32_top2 | 33.4187     |
| delta16_top8 | 33.5063     |
| delta32_top4 | 33.6291     |
| delta16_top4 | 33.8603     |
| delta8_top12 | 34.0182     |

你说 restart_lora 是 32.25。这里最好的 `delta8_top2` 其实和 baseline 几乎打平，略好 0.01，但这个量级基本可以视为噪声。真正有信息量的是：**top_k 越大、delta 越大，普遍越差。** 这说明当前 rank reallocation 不是在稳定地找到更优结构，而是在给 restart_lora 增加结构扰动。

------

# 1. 先定位：当前方法最大的问题不是 credit，而是“删 rank”

你的 baseline restart_lora 做的是：

[
W=BA \rightarrow W=B'A'
]

如果 rank 不变，SVD refactorization 基本保持 (W) 不变，只是改变 (B,A) 的参数化。lowrank_benchmark.pdf 里 weight refactorization 的理论也是建立在 **保持 (W=BA) 不变，只重平衡 (B,A)** 的基础上；论文里写到用 (B'=U\sqrt{\Sigma})、(A'=\sqrt{\Sigma}V^\top) 改善 conditioning，并且每 200 updates refactorize 一次能够稳定提升效果 。

但你的 rank allocation 不一样。当前 `refactorize_with_rank` 会先构造：

[
W = BA
]

然后做 SVD，再按 `new_rank` 截断。如果 `new_rank < old_rank`，这一步不再是 refactorization，而是 **rank compression**，会丢掉尾部 singular components。代码里确实是根据 `target_rank` 取前 `svd_rank` 个奇异值，然后重建新的 (B,A)；新增 rank 时才额外拼接随机 channel 。

这意味着：

> restart_lora 是“函数基本不变 + 优化状态重启”；
> rank_allocation_lora 是“函数被压缩扰动 + 优化状态重启”。

所以你看到效果普遍更差很正常。**只要 remove 侧选错一次，就会永久删掉某些有用方向。** 而 LLM pretraining 早期的信号非常 noisy，当前分配器又每次从 bottom score 模块直接拿 rank，这个风险非常大。

------

# 2. 当前实现里有几个高概率导致效果差的点

## 2.1 当前 score 是乘法，太容易把“有潜力但当前受限”的模块误删

现在的 score 是：

[
Q_g=\text{eff_ema}_g \times \text{probe_ema}_g \times \frac{1}{\sqrt{r_g}}
]

代码也是直接这样算的：

```python
self.score = self.eff_ema * self.probe_ema * scarcity
```



这个问题很大。因为乘法有一个坏性质：**任意一项接近 0，整体就是 0。**

但在 rank allocation 里，低 credit 可能有两种完全相反的含义：

1. 这个模块真的不重要；
2. 这个模块 rank 太低，表达不出有效 update，所以当前 credit 低。

第二种模块恰恰可能需要加 rank，但当前乘法 score 会把它排到底部，甚至拿它去减 rank。

所以 current score 不适合同时决定 add 和 remove。**add score 和 remove score 必须分开。**

------

## 2.2 probe 没有正交化，所以它测到的可能只是 raw gradient norm

当前 probe 是：

[
B_p=0,\quad A_p\sim \mathcal N(0,\sigma^2)
]

forward 中加：

[
B_p A_p
]

因为 (B_p=0)，forward 不变。然后用：

[
|\nabla_{B_p}L|/|A_p|
]

作为 probe score。代码确实是随机生成 `A_probe`，`B_probe` 置零，然后用 `B_probe.grad.norm() / A_probe.norm()` 作为 score 。

这个工程实现是对的，但它不是“rank saturation residual”。因为 (A_p) 是普通随机方向，没有从当前 (A) 的 row-space 中正交出去。于是 probe 很可能测到的是：

[
|G A_p^\top|
]

也就是这个矩阵的 raw gradient magnitude，而不是“当前 low-rank 子空间表达不了的梯度”。

真正想测的是：

[
A_p \perp \operatorname{row}(A)
]

也就是新增 rank 应该探索当前 (A) 还没有覆盖的方向。否则一个已经 rank 够用、但 gradient norm 很大的模块，也会拿到高 probe score。

------

## 2.3 当前 remove 规则没有检查“删掉 rank 会损失多少信息”

当前 `allocate_rank_budget` 只是按 score 排序，top 加 rank，bottom 减 rank，并检查 min/max/hysteresis。它没有检查被删模块的 SVD tail energy 。

但删 rank 时真正应该问的是：

[
\text{如果从 } r \text{ 减到 } r-\Delta r,\quad
\frac{\sqrt{\sum_{j=r-\Delta r+1}^{r}\sigma_j^2}}{\sqrt{\sum_{j=1}^{r}\sigma_j^2}}
\text{ 有多大？}
]

如果这个 tail energy 不小，就说明删掉的是有实际函数贡献的方向。此时即使这个模块 credit 低，也不能删。

这就是你当前方法最可能输给 restart_lora 的核心原因：**restart_lora 不删信息；你的方法在删信息。**

------

## 2.4 新增 rank 是随机初始化，没有利用 probe 方向

当前 rank 增长时，如果 `target_rank > svd_rank`，代码会给 extra_A 和 extra_B 都用小随机数初始化 。

这会带来两个问题：

第一，它没有利用刚刚 probe 发现的方向。你 probe 的意思是“这个新增方向可能有用”，但真正加 rank 的时候却随机加了别的方向。

第二，extra_A 和 extra_B 都非零，虽然 std 很小，但仍然会给 (W) 加一个小随机扰动。更稳的做法是：

[
A_{\text{new}} = A_{\text{probe}},\quad B_{\text{new}} = 0
]

这样 forward 完全不变，但 backward 立刻有：

[
\nabla_{B_{\text{new}}}L = G A_{\text{probe}}^\top
]

也就是说，新增 rank 从第一步就沿着 probe 发现的方向学习。

------

# 3. 你现在的 sweep 说明什么？

这次结果其实很有价值。

**现象 1：best 是 delta8_top2。**
说明小幅移动 rank 基本不破坏训练，甚至可能略有帮助。

**现象 2：delta32 和 top8/top12 明显差。**
说明大规模 rank 搬运会破坏 restart_lora 的稳定性。

**现象 3：各 run 前 1000、2000、3000 step 非常接近，后期差距拉大。**
说明不是初始化或训练框架彻底错，而是 rank reallocation 逐渐积累了结构损伤。

所以我的判断是：

> 不是 idea 不行，而是当前版本把 rank allocation 做成了“无保护的 rank trading”。
> 现在应该从 aggressive reallocation 改成 conservative protected reallocation。

------

# 4. 我建议的算法改进

------

## 4.2 第二条：rank allocation 不要每次 restart 都动

你可以继续每 (T=500) 做：

- weight refactorization；
- momentum reset。

但 rank allocation 应该更低频。例如：

[
T_{\text{restart}}=500
]

[
T_{\text{rank-alloc}}=2000
]

也就是每 4 次 restart 才重新分配一次 rank。

建议第一版：

```text
restart every 500
rank allocation every 2000
rank_allocation_start_step = 4000 或 5000
```

原因是早期 credit/probe 极不稳定，uniform rank 本身已经很强。不要在模型还没形成稳定结构前搬 rank。

------

## 4.3 第三条：把 add score 和 remove score 分开

不要再用一个乘法 score 同时决定加和减。

我建议定义两个分数。

### Add score：谁值得加 rank？

# [ Q_g^{add}

z_{\text{type}}\left(\log(S_g^{orth}+\epsilon)\right)
+
\lambda_c z_{\text{type}}\left(\log(E_g+\epsilon)\right)
+
\lambda_r \frac{1}{\sqrt{r_g}}
]

其中：

- (S_g^{orth})：正交 rank probe score；
- (E_g)：credit efficiency；
- (z_{\text{type}})：在同一 module type 内做 robust z-score，比如 attention 内部比、MLP 内部比；
- (\frac{1}{\sqrt{r_g}})：rank scarcity。

### Remove score：谁可以安全减 rank？

# [ Q_g^{remove}

## z_{\text{type}}\left(\log(E_g+\epsilon)\right) + z_{\text{type}}\left(\log(S_g^{orth}+\epsilon)\right)

\lambda_{\text{tail}}\cdot \text{TailEnergy}_g
]

remove 应该选 **低 remove score** 的模块，并且必须满足：

[
\text{TailEnergy}*g(\Delta r) < \tau*{\text{tail}}
]

比如：

```text
tail_threshold = 1e-3 或 5e-3
```

这条非常重要。没有 tail protection，rank deletion 就是在盲删模型函数。

------

## 4.4 第四条：probe 必须正交化

当前 probe 是随机 (A_p)。应该改成：

[
A_p \leftarrow A_p - A_p V V^\top
]

其中 (V=\operatorname{orth}(A^\top))，表示当前 (A) 的 row-space。

工程上可以这么写：

```python
@torch.no_grad()
def orthogonal_random_rows(A, probe_rank, sigma):
    # A: [r, in_features]
    # return P: [probe_rank, in_features], approximately orthogonal to row(A)
    device, dtype = A.device, A.dtype
    P = torch.randn(probe_rank, A.shape[1], device=device, dtype=torch.float32)

    # row-space basis of A
    # Q: [in_features, r_eff]
    Q, _ = torch.linalg.qr(A.detach().float().T, mode="reduced")

    # project P away from row(A)
    P = P - (P @ Q) @ Q.T

    # normalize
    P = P / (P.norm(dim=1, keepdim=True) + 1e-12)
    P = P * sigma
    return P.to(dtype=dtype)
```

然后：

```python
self.B_probe = nn.Parameter(torch.zeros(out_features, probe_rank, ...))
self.A_probe = nn.Parameter(orthogonal_random_rows(self.lora_A, probe_rank, sigma))
```

这样 probe 才更接近“新增 rank 是否提供当前子空间外的能力”。

进一步可以做 left probe：

[
B_p \perp \operatorname{col}(B),\quad A_p=0
]

然后看：

[
|\nabla_{A_p}L|
]

最终：

[
S_g^{orth}=\sqrt{S_{A,g}S_{B,g}}
]

或者：

[
S_g^{orth}=\max(S_{A,g},S_{B,g})
]

第一版只做 right-orthogonal (A_p) 就够。

------

## 4.5 第五条：新增 rank 用 probe 方向初始化，而不是随机初始化

现在 extra_A/extra_B 是随机的。建议改成：

```python
extra_A = module.last_A_probe.clone()
extra_B = torch.zeros(out_features, extra_rank)
```

这样新增 rank 不改变当前函数：

[
B_{\text{new}}A_{\text{new}}=0
]

但下一步有有效梯度：

[
\nabla_{B_{\text{new}}}L=G A_{\text{new}}^\top
]

这比随机初始化更符合 probe 的目的。

如果 `extra_rank > probe_rank`，可以：

1. probe_rank 设置等于 delta_rank；
2. 或者重复做多个 probe；
3. 或者用 orthogonal random 补齐剩余部分。

------

## 4.6 第六条：删除 rank 前先检查 SVD tail energy

在 `refactorize_with_rank` 前，加一个函数：

```python
@torch.no_grad()
def rank_tail_energy(module, new_rank):
    W = module.lora_B.detach().float().mm(module.lora_A.detach().float())
    S = torch.linalg.svdvals(W)
    total = S.pow(2).sum().sqrt()
    if new_rank >= len(S):
        return 0.0
    tail = S[new_rank:].pow(2).sum().sqrt()
    return (tail / (total + 1e-12)).item()
```

分配时：

```python
if rank_tail_energy(remove_module, new_rank) > tail_threshold:
    skip this remove_module
```

这会极大降低损伤。

更稳的版本是不看整个 (W)，只看当前 old_rank 内最后 delta 个奇异值：

[
\frac{\sqrt{\sum_{j=r-\Delta r+1}^{r}\sigma_j^2}}
{\sqrt{\sum_{j=1}^{r}\sigma_j^2}}
]

------

## 4.7 第七条：每次只移动很少 rank

根据你的 sweep，当前最好的已经是 `delta8_top2`。我建议下一轮更保守：

```text
delta_rank = 4 或 8
top_k = 1 或 2
min_ratio = 0.1875
max_ratio = 0.3125
hysteresis = 0.5
rank_allocation_start_step = 4000
rank_allocation_interval = 2000
probe_rank = 4 或 8
```

现在的 `[0.125, 0.5]` 太宽了。初始是 0.25，如果允许到 0.125 和 0.5，等于某些矩阵 rank 减半，某些矩阵 rank 翻倍。对 60M 这种小模型来说，这个扰动太大。

第一版应该只允许：

[
r_g \in [0.1875d,\ 0.3125d]
]

也就是围绕 0.25 小范围调整。

------

# 5. 推荐的新 allocation 逻辑

不要再：

```python
top score add
bottom score remove
```

而是：

```text
1. 计算 add_score
2. 计算 remove_score
3. add candidate 必须 add_score 高
4. remove candidate 必须 remove_score 低
5. remove candidate 必须 tail_energy 足够小
6. add_score - remove_score 必须超过 margin
7. 每轮最多移动 top_k 个小 delta
```

伪代码：

```python
def allocate_rank_budget_v2(modules, delta=8, top_k=2):
    add_candidates = sorted(
        modules,
        key=lambda m: m.add_score,
        reverse=True
    )

    remove_candidates = sorted(
        modules,
        key=lambda m: m.remove_score
    )

    moves = []
    for add_m in add_candidates:
        if len(moves) >= top_k:
            break
        if add_m.rank + delta > add_m.r_max:
            continue

        for rem_m in remove_candidates:
            if rem_m is add_m:
                continue
            if rem_m.rank - delta < rem_m.r_min:
                continue

            new_rank = rem_m.rank - delta
            tail = rank_tail_energy(rem_m, new_rank)
            if tail > tail_threshold:
                continue

            if add_m.add_score <= rem_m.remove_score + margin:
                continue

            moves.append((rem_m, add_m, delta))
            rem_m.blocked = True
            break

    return moves
```

这个版本会少动很多 rank，但每次移动更可信。

